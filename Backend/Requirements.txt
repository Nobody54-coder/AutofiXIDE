# FastAPI & WebSocket support
fastapi==0.110.0
uvicorn[standard]==0.29.0

# AI-powered debugging (OpenAI API)
openai==1.12.0

# Code execution and sandboxing
pyflakes==3.2.0  # Static analysis for error detection
pyright==1.1.350  # Type checking
executing==2.0.1  # Extracts live code execution info

# Security enhancements
pyjwt==2.8.0  # JWT-based authentication (if needed)
python-dotenv==1.0.1  # Load environment variables
pydantic-settings==2.2.1  # Structured settings management

# Testing & debugging
pytest==8.1.1
httpx==0.27.0
websockets==12.0

# Optional (for Docker-based execution)
docker==7.0.0
